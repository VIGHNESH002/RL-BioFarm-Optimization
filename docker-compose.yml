version: '3.8'
services:
  api:
    build: .
    ports: ["8000:8000"]
    command: uvicorn api:app --host 0.0.0.0 --port 8000
  ui:
    build: .
    ports: ["8501:8501"]
    command: streamlit run app.py
    depends_on:
      - api